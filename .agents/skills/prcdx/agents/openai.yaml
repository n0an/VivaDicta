interface:
  display_name: "Open Pull Request (Codex)"
  short_description: "Create a GitHub Pull Request in Codex with Claude and Codex review requests and standard GitHub polling"
  brand_color: "#7C3AED"
  default_prompt: "Use $prcdx on this project."

policy:
  allow_implicit_invocation: false
