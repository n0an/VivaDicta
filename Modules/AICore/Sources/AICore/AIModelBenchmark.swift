// Generated by scripts/ai-provider-eval/export.py - do not edit by hand.
// Regenerate: cd scripts/ai-provider-eval && ./eval.py --emit-swift <this file>

import Foundation

/// One model's standing in the transcription-cleanup benchmark.
///
/// Public because iOS ships this inside the AICore module; in the macOS app
/// target the modifier is simply redundant, which keeps one emitter for both.
public struct AIModelBenchmark: Identifiable, Hashable, Sendable {
    /// Position in the ranking, 1-based.
    public let rank: Int
    /// Provider name as the app shows it, e.g. "Grok (xAI)".
    public let provider: String
    /// Model id exactly as it is selected in the app.
    public let model: String
    /// Mean of the cleanup cases, 0-10.
    public let quality: Double
    /// Median seconds for one enhancement.
    public let seconds: Double
    /// True when the provider serves this model without a paid plan.
    public let freeTier: Bool

    public var id: String { "\(provider)/\(model)" }
}

/// The best models for cleaning up dictation, measured rather than guessed.
///
/// Only the top 10 ship in the app; the full table of
/// 59 models is on the website, which is also where the
/// method is written up.
public enum AIModelBenchmarkCatalog {
    /// URL of the full ranking.
    public static let fullTableURL = URL(string: "https://vivadicta.com/docs/ai-model-rankings")!

    /// ISO date the measurements were taken. Shown so a stale table is visibly
    /// stale rather than quietly wrong.
    public static let measuredAt = "2026-09-12"

    public static let top: [AIModelBenchmark] = [
        AIModelBenchmark(
            rank: 1,
            provider: "Gemini",
            model: "gemini-3.8-flash",
            quality: 9.7,
            seconds: 0.87,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 2,
            provider: "OpenAI",
            model: "gpt-5.4-mini",
            quality: 9.7,
            seconds: 0.89,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 3,
            provider: "OpenAI",
            model: "gpt-4.1-mini",
            quality: 9.6,
            seconds: 0.74,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 4,
            provider: "Anthropic",
            model: "claude-haiku-4-5",
            quality: 9.5,
            seconds: 0.74,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 5,
            provider: "Vercel AI Gateway",
            model: "anthropic/claude-haiku-4.5",
            quality: 9.6,
            seconds: 0.78,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 6,
            provider: "Grok (xAI)",
            model: "grok-4.20-non-reasoning",
            quality: 9.6,
            seconds: 0.79,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 7,
            provider: "OpenAI",
            model: "gpt-5.4",
            quality: 9.5,
            seconds: 0.84,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 8,
            provider: "Gemini",
            model: "gemini-3.7-flash",
            quality: 9.5,
            seconds: 0.9,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 9,
            provider: "Gemini",
            model: "gemini-3.5-flash",
            quality: 9.6,
            seconds: 0.93,
            freeTier: false
        ),
        AIModelBenchmark(
            rank: 10,
            provider: "OpenRouter",
            model: "anthropic/claude-haiku-4.5",
            quality: 9.6,
            seconds: 1.02,
            freeTier: false
        )
    ]
}
